export default function MaintenancePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-100 px-4 py-10">
      <section className="w-full max-w-md rounded border border-slate-300 bg-white p-6">
        <p className="text-sm font-semibold text-slate-600">Provider Tracker</p>
        <h1 className="mt-2 text-2xl font-semibold text-slate-900">Temporarily unavailable</h1>
        <p className="mt-2 text-sm text-slate-600">Maintenance is in progress. Try again in a few minutes.</p>
      </section>
    </main>
  );
}
